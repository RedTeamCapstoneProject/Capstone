{
  "framework": null,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/reset-password", "dest": "/reset-password.html" },
    { "handle": "filesystem" }
  ]
}